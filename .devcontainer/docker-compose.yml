version: "3.8"

services:
  devcontainer:
    build:
      context: ..        # ← . から .. に変更
      dockerfile: .devcontainer/Dockerfile   # ← パスも合わせて修正
    container_name: tv-snapshot-dev
    command: sleep infinity
    volumes:
      - ../:/workspace:cached
    working_dir: /workspace